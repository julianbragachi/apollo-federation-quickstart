extend schema
    @link(url: "https://specs.apollo.dev/federation/v2.0",
       import: ["@key", "@shareable", "@tag", "@external", "@provides"])

type Query {
  foo: Bar 
}

type User @key(fields: "email") {
  email: ID
  id: String @external
}

type Bar {
  user: User @provides(fields: "id")
}